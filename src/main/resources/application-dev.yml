spring:
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.sicredi.voting: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE   # mostra os parâmetros