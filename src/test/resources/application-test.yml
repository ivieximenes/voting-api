spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop 

sicredi:
  voting:
    default-session-duration: 60s
  app:
    base-url: http://localhost:8080
  member-validation:
    enabled: false
    base-url: http://localhost:1
    connect-timeout-ms: 1000
    read-timeout-ms: 1000
